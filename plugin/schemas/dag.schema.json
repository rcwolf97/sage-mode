{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "sage-mode task DAG",
  "type": "object",
  "required": ["version", "sprint", "base", "nodes"],
  "additionalProperties": false,
  "properties": {
    "version": { "const": 1 },
    "sprint": { "type": "string", "pattern": "^[0-9]{2}(-[a-z0-9-]+)?$" },
    "base": { "type": "string", "description": "git ref the sprint branches from" },
    "profile": { "enum": ["web", "api", "cli", "ai-product"] },
    "constraints": {
      "type": "array",
      "maxItems": 40,
      "items": { "type": "string", "minLength": 10 },
      "description": "Rules binding every node: version floors, dependency limits, naming/copy rules, exact values."
    },
    "nodes": {
      "type": "array",
      "minItems": 1,
      "maxItems": 60,
      "items": {
        "type": "object",
        "required": ["id", "title", "role", "owns", "acceptance", "verify", "risk", "slice"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "pattern": "^n[0-9]+$" },
          "title": { "type": "string", "minLength": 8, "maxLength": 120 },
          "role": { "enum": ["frontend", "backend", "data", "infra", "ai", "design"] },
          "depends_on": {
            "type": "array",
            "items": { "type": "string", "pattern": "^n[0-9]+$" },
            "default": []
          },
          "owns": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string" }
          },
          "reads": {
            "type": "array",
            "items": { "type": "string" },
            "default": []
          },
          "acceptance": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 10 }
          },
          "verify": { "type": "string", "minLength": 1 },
          "risk": { "enum": ["low", "medium", "high"] },
          "slice": { "enum": ["vertical", "prefactor", "refactor-batch"] },
          "design": { "enum": ["none", "required"], "default": "none" },
          "model": { "type": ["string", "null"], "default": null },
          "notes": { "type": "string" },
          "interfaces": {
            "type": "object",
            "additionalProperties": false,
            "description": "Contract identifiers this node consumes from / produces for other nodes, e.g. auth.verifyToken.",
            "properties": {
              "consumes": {
                "type": "array",
                "items": { "type": "string", "minLength": 1, "maxLength": 40 }
              },
              "produces": {
                "type": "array",
                "items": { "type": "string", "minLength": 1, "maxLength": 40 }
              }
            }
          }
        }
      }
    }
  }
}
