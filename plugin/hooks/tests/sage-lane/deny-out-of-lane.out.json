{"permission": "deny", "agent_message": "[lane] src/web/x.ts is outside owns ['src/api/**']. Request a lane amendment rather than retry.", "user_message": "[lane] src/web/x.ts is outside owns ['src/api/**']. Request a lane amendment rather than retry."}
